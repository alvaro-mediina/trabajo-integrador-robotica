<launch>
    <set_env name="GZ_SIM_RESOURCE_PATH" value="$(find-pkg-share my_robot_bringup)/worlds/models:$(find-pkg-share my_robot_description)/.." />

    <let name="urdf_path"
         value="$(find-pkg-share my_robot_description)/urdf/my_robot.urdf.xacro" />
    <let name="rviz_config_path"
         value="$(find-pkg-share my_robot_description)/rviz/slam_rviz.rviz" />

    <!-- ============ Argumentos ============ -->
    <arg name="world_path" default="$(find-pkg-share my_robot_bringup)/worlds/bookstore_final.sdf" />

    <!-- Punto de spawn del robot en el mundo -->
    <arg name="spawn_x" default="0.0" />
    <arg name="spawn_y" default="0.0" />

    <!-- Estado del robot -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description"
               value="$(command 'xacro $(var urdf_path)')" />
        <param name="use_sim_time" value="true"/>
    </node>

    <!-- Gazebo: SOLO servidor (física + sensores), sin GUI acá -->
    <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
        <arg name="gz_args" value="$(var world_path) -r -s" />
    </include>

    <!-- GUI de Gazebo como proceso aparte, conectada al servidor de arriba -->
    <executable cmd="gz sim -g" output="screen" />

    <!-- Spawn del robot -->
    <node pkg="ros_gz_sim" exec="create"
          args="-topic robot_description -x $(var spawn_x) -y $(var spawn_y) -z 0.1" />

    <!-- Puente ROS <-> Gazebo -->
    <node pkg="ros_gz_bridge" exec="parameter_bridge">
        <param name="config_file" value="$(find-pkg-share my_robot_bringup)/config/config.yaml" />
    </node>

    <!-- RViz -->
    <node pkg="rviz2" exec="rviz2" output="screen"
        args="-d $(var rviz_config_path)">
        <param name="use_sim_time" value="true" />
    </node>

        <!-- SLAM Toolbox -->
    <node pkg="slam_toolbox" exec="async_slam_toolbox_node" name="slam_toolbox" output="screen">
        <param from="$(find-pkg-share my_robot_bringup)/config/slam_params.yaml" />
        <param name="use_sim_time" value="true" />
    </node>

    <!-- Lifecycle Manager: lleva slam_toolbox de unconfigured -> active -->
    <node pkg="nav2_lifecycle_manager" exec="lifecycle_manager" name="lifecycle_manager_slam" output="screen">
        <param name="use_sim_time" value="true" />
        <param name="autostart" value="true" />
        <param name="node_names" value="[slam_toolbox]" />
    </node>
    </launch>